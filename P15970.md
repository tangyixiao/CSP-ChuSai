# P15970 树上检查

给定一棵 $n$ 个结点的树和一个正整数 $k$（$1 \le k \le 4$）。你需要选择 $k$ 个结点设置为检查点，并选择若干条简单路径，使得这些简单路径**经过**这 $k$ 个结点（注意：并不是要求同一条路径同时经过所有结点），并使简单路径条数最少。

请你对于所有选择结点的方案，计算这些方案所需的最少简单路径条数的**总和**，答案对 $998244353$ 取模。

阅读下面的程序代码，在画线处填入正确的选项。

\`\`\`cpp
#include <bits/stdc++.h>
#define N 200005
#define int long long
#define mod 998244353
using namespace std;

vector<int> ve[N];
int dp[N][5];

void dfs(int x, int fa) {
    ________①________
    for (int i : ve[x]) {
        if (i == fa) continue;
        dfs(i, x);
        for (int j = 1; j <= 4; j++) {
            ________②________
        }
    }
}

int cnt(int x, int fa, int op) {
    int ret = 0;
    for (int i : ve[x]) {
        if (i == fa) continue;
        ________③________
    }
    map<int, int> mp;
    mp[0] = 1;
    for (int i : ve[x]) {
        if (i == fa) continue;
        for (int j = 1; j < op; j++) {
            ret += dp[i][j] * (mp[op - j] + mp[op - j - 1]) % mod;
            ret %= mod;
        }
        for (int j = 1; j < op; j++) {
            ________④________
        }
    }
    return ret;
}

void solve() {
    memset(dp, 0, sizeof dp);
    int n, k;
    cin >> n >> k;
    for (int i = 1; i <= n; i++) ve[i].clear();
    for (int i = 1; i < n; i++) {
        int u, v;
        cin >> u >> v;
        ve[u].push_back(v);
        ve[v].push_back(u);
    }
    if (k <= 2) {
        if (k == 1) cout << n << '\n';
        else cout << n * (n - 1) / 2 % mod << '\n';
        return;
    }
    dfs(1, 0);
    if (k == 3) {
        int total = n * (n - 1) * (n - 2) / 6 % mod;
        cout << (total * 2 % mod - cnt(1, 0, 3) + mod) % mod << '\n';
    } else {
        int total = 1;
        for (int i = 0; i < 4; i++) total = total * (n - i) % mod;
        ________⑤________
        cout << (total * 2 % mod - cnt(1, 0, 4) + mod) % mod << '\n';
    }
}

signed main() {
    int t;
    cin >> t;
    while (t--) solve();
    return 0;
}
\`\`\`

---

**① 处应填：**  
A. \`dp[x][0] = 1;\`  
B. \`dp[x][1] = 1;\`  
C. \`dp[x][1] = 0;\`  
D. \`dp[x][0] = 0;\`

**② 处应填：**  
A. \`dp[x][j] = (dp[x][j] + dp[i][j]) % mod;\`  
B. \`dp[x][j] = (dp[x][j] + dp[i][j - 1] + dp[i][j]) % mod;\`  
C. \`dp[x][j] = (dp[x][j] + dp[i][j] + dp[i][j + 1]) % mod;\`  
D. \`dp[x][j] = (dp[x][j] + dp[i][j - 1]) % mod;\`

**③ 处应填：**  
A. \`ret = cnt(i, x, op);\`  
B. \`ret += cnt(i, x, op);\`  
C. \`ret = cnt(i, x, op) % mod;\`  
D. \`ret += cnt(i, x, op); ret %= mod;\`

**④ 处应填：**  
A. \`mp[j] = (mp[j] + dp[i][j - 1]) % mod;\`  
B. \`mp[j] = (mp[j] + dp[x][j]) % mod;\`  
C. \`mp[j] = (mp[j] + dp[i][j]) % mod;\`  
D. \`mp[j] = (mp[j] + dp[i][j] + dp[i][j - 1]) % mod;\`

**⑤ 处应填：**  
A. \`for (int i = 2; i <= 4; i++) while (total % i == 0) total /= i;\`  
B. \`total = total / 24 % mod;\`  
C. \`total = total * 24 % mod;\`  
D. \`for (int i = 2; i <= 4; i++) total /= i;\`

---

### 答案

| 题号 | 答案 |
|------|------|
| ①    | B    |
| ②    | B    |
| ③    | D    |
| ④    | C    |
| ⑤    | A    |

---

### 详细解析

**① 选 B**  
\`dp[x][1] = 1\` 表示：在子树 $x$ 中，只选 $x$ 自身作为链上的一个点，这是一种合法方案。这是所有后续 DP 合并的起点。

**② 选 B**  
合并子节点 $i$ 时，选出的 $j$ 个点有两种来源：全部来自子节点 $i$ 的子树，或者子节点 $i$ 贡献 $j-1$ 个并加上 $x$ 自身。因此转移为：

\`\`\`cpp
dp[x][j] = (dp[x][j] + dp[i][j - 1] + dp[i][j]) % mod;
\`\`\`

**③ 选 D**  
\`cnt(x, fa, op)\` 需要累加所有儿子子树中的答案，并及时取模。

**④ 选 C**  
\`mp[j]\` 表示已经处理过的子树中选出 $j$ 个有祖先后代关系点的方案数，所以加入当前子树的 \`dp[i][j]\`。

**⑤ 选 A**  
\`total = n(n-1)(n-2)(n-3)\`，需要除以 $24$ 得到 $\binom n4$。这里通过逐步约掉因子避免直接进行模意义下的除法。

---

**核心结论：**

- 当 $k=1$ 时，答案为 $n$。
- 当 $k=2$ 时，答案为 $\binom n2$。
- 当 $k=3$ 时，若三个点共线，则需要 $1$ 条路径；否则需要 $2$ 条路径。
- 当 $k=4$ 时，若四个点共线，则需要 $1$ 条路径；否则需要 $2$ 条路径。

因此，问题转化为：**计算三点共线和四点共线的方案数。**
